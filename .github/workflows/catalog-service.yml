name: Catalog Service CI/CD

on:
  push:
    branches: [ "main", "feature/**" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  compile:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v4
      - uses: actions/setup-java@v3
        with:
          distribution: temurin
          java-version: 17
      - uses: gradle/gradle-build-action@v2
        id: setup-gradle
        with:
          gradle-version: current
      - name: assembling the service
        run: ./gradlew --parallel --no-daemon compileJava
  tests:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v4
      - uses: actions/setup-java@v3
        with:
          distribution: temurin
          java-version: 17
      - uses: gradle/gradle-build-action@v2
        id: setup-gradle
        with:
          gradle-version: current
      - name: testing the service
        run: ./gradlew --parallel --no-daemon test
      - name: test results publishing
        run: echo Test Results Published
  build:
    runs-on: ubuntu-latest
    needs: [ "compile", "tests" ]
    steps:
      - name: checkout
        uses: actions/checkout@v4
      - uses: actions/setup-java@v3
        with:
          distribution: temurin
          java-version: 17
      - uses: gradle/gradle-build-action@v2
        id: setup-gradle
        with:
          gradle-version: current
      - name: building the service
        run: ./gradlew --parallel --no-daemon build -x check
  delivery:
    runs-on: ubuntu-latest
    needs: [ "build" ]
    steps:
      - name: checkout
        uses: actions/checkout@v4
      - uses: actions/setup-java@v3
        with:
          distribution: temurin
          java-version: 17
      - uses: gradle/gradle-build-action@v2
        id: setup-gradle
        with:
          gradle-version: current
      - name: docker image building and publishing
        run: ./gradlew --parallel --no-daemon jibDockerBuild -x check
      - name: Scan image
        run: echo grype analysis
        uses: anchore/scan-action@v3
        with:
          image: "gcr.io/template-ase/catalog-service"
          fail-build: true
          severity-cutoff: critical
